IDENTITY-UNRESOLVED OFFLINE TOOLING CHECKPOINT — NOT EXECUTION AUTHORITY
=====================================================================

Fail-closed identity state
--------------------------
The production contract and candidate environment contain these invalid
sentinels:

  Candidate Core source:
    __FINAL_SIGNED_CORE_SHA__
  Exact-SHA Core safety run:
    __FINAL_EXACT_SHA_CORE_CI_RUN_ID__
  Candidate release:
    30.1.5
  Expected PR base:
    19baffef25af36e177db2975780e0641b59753aa
  Expected pr-gate.yml SHA-256:
    24c14f2fe4bd7b25de38e71a80bf05efcec00d2b3009c3efd4ad20b90bbda869

The sentinels are not valid source/run identities. The shared exact-identity
predicate rejects them before either live phase can pass preflight. A later,
separate, reviewed identity-only commit must replace both sentinels with one
Blackcoin-Dev-signed source H and its completed-success exact-SHA run R, then
repin the signed adapter, bundle, OCI graph, image, provenance, and six-binary
identities from the same ordinary-user artifact.

The offline suite copies the contract and replaces only those two sentinels
with deterministic test identities:

  Synthetic source H:
    0123456789abcdef0123456789abcdef01234567
  Synthetic exact-SHA run R:
    42424242424

Every derived test prefix, archive, image tag, artifact name, candidate
identity record, and Core-CI record is regenerated from that copied contract.
The production files remain unresolved. The immutable v30.1.4 source,
manifest, image/config ID, historical snapshot names, rollback paths, and
final-canary byte pins remain unchanged.

Generic product boundary exercised offline
-------------------------------------------
PASS  Nonzero unresolved, retained, quarantine, blocking, raw, retired, and
      resolved counters are typed telemetry, not enable/health vetoes and not
      cross-version equality predicates.
PASS  Coherent, database-unambiguous typed gates accept create_new_anchor,
      refresh_same_anchor, wait_for_live, wait_for_next_tip, and
      relay_existing while requiring zero authoritative unsafe claims and
      components.
PASS  Recovery compatibility `.components` telemetry may differ from the full
      audit `component_details` inventory; the full inventory still rejects a
      txid duplicated across components.
PASS  Phase-A schema 4 accepts at least three advancing observations, zero or
      more newly authored claims, multiple authenticated authored components,
      multiple claims on one sampled tip, and exact plural anchor evidence.
PASS  Both Phase-A terminal cuts independently pass the full typed observation
      contract, including selected-component authentication and raw-mempool
      binding. Cross-cut equality binds the chain/wallet cut, recovery-policy
      authority, and the explicit operational gate projection only. Full audit
      component details, unanchored/history counters, and candidate-state
      fingerprint may drift without becoming host-side health predicates.
PASS  All ten pre-release/pre-destroy/post-destroy authority rechecks retain
      immutable invocation, nonpublication, anchor, and observer evidence with
      exact sequence/stage/snapshot and hash bindings.
PASS  Bounded liveness permits short zero-hash wait/relay transitions, credits
      only action-appropriate progress, tracks first-seen authenticated live
      family txids, and rejects replay, rewritten lineage, family-generation
      churn, and unbounded no-progress action alternation.
PASS  Phase B treats immutable v30.1.4 `.enabled` as telemetry. Node 27 is a
      regular-PoW canary, so candidate PoW intent is explicitly enabled and
      both progress and final evidence are active-only. The enable RPC alone is
      not treated as Core liveness evidence.
PASS  Configured-future payout may be empty for retained wait/relay/refresh
      work. When it is nonempty, exact equality and wallet ownership remain
      mandatory. This does not claim selected-family persisted-payout proof.
PASS  The corrected implicit metadata-absent root contract binds zero lineage
      defaults, the exact QQP2/3/4 origin/input tuples, authored and carrier
      metadata, Core disposition/revalidation semantics, unspent-anchor
      proof-evaluation status, and the singleton-QQP2 authored-tip rule.
PASS  PoS remains explicitly active in Phase B; no recovery/cleanup payment,
      abandon, raw broadcast, host repair, data rewind, or old-Core restart is
      permitted after promotion.

Exact offline validation
------------------------
PRESEAL-INPUT  The exact six non-narrative payloads are frozen: environment,
               contract, Phase A, Phase B, tests, and verifier. The digest of
               their ordered sha256sum list, in manifest order with README and
               VALIDATION omitted, is
               48bb93d2daa00f059fb2ec0db110d55c3a74b37d2d38cda78b891128f6f3f7f8.
PASS  Bash syntax for every .sh/.sh.inc input and candidate.env.example.
PASS  Full ShellCheck -x for every package shell input, with no findings.
PASS  Git diff whitespace validation.
PRESEAL-PROCEDURE  SHA256SUMS mechanically binds the six reviewed payloads with
                   final README and VALIDATION bytes. A completed review
                   requires strict verification of that exact eight-payload
                   manifest, exact topology and static gates, a local full
                   sealed 528-assertion replay with all nine file hashes
                   unchanged, and an independent post-seal review recorded
                   outside this package. This file intentionally does not
                   attest or predict that external result and does not embed
                   volatile fixture paths.

The two verifier routing fixtures use the deterministic copied contract and
therefore reach the named unknown-mode and missing-root branches. A separate
fixture proves that the unrepinned production contract fails exact identity
resolution.

External authority blockers
---------------------------
BLOCKED  Production Core H and exact-SHA run R remain invalid sentinels. No
         signed successor or hosted run is accepted by these production bytes.
BLOCKED  The signed adapter/workflow commit, candidate packaging run and
         attempt, bundle, OCI archive/graph, image, provenance, and six-binary
         identities remain unresolved placeholders.
BLOCKED  Live Compose/runtime hashes, Phase-A evidence identity, separate
         Phase-A and Phase-B confirmations, independent identity-bound review,
         and explicit canary authorization remain external and unresolved.

Revoked predecessor boundary
----------------------------
Signed commit 309731e3340f380e48cb67f94a243725465420fb is retained only as
an immutable diagnostic source oracle for the typed RPC/API shape exercised by
the offline suite. It is a revoked, failed predecessor. It is never substituted
into the production identity sentinels and supplies no Core, package, preseal,
canary, or rollout authority.

Seal and authorization boundary
-------------------------------
The checksum manifest is an identity-neutral offline tooling-integrity seal.
Even when strict verification and the sealed replay pass, it does not prove
Core correctness or grant execution authority. With unresolved H/R sentinels
and unresolved candidate artifact identities, Phase A, Phase B, build, canary,
rollout, and fleet mutation remain impossible by design.

Not executed or claimed
-----------------------
No Docker or Compose command, workflow dispatch, GitHub release/build action,
artifact build/download/load, wallet RPC, container change, ZFS operation, live
canary, or fleet action was performed by the offline validation described here.
No successful Core candidate, identity-bound package, artifact, image, release,
PoS fleet count, PoW fleet count, or node30 Free Claim health is claimed by this
offline checkpoint.
