PROVISIONAL SOURCE/RUN-BOUND CHECKPOINT — NOT EXECUTION AUTHORITY
================================================================

NONDEPLOYABLE CHECKPOINT
------------------------
These nine-path two-phase canary bytes provisionally bind:

  Candidate Core source:
    a0695f22740e111d0487a194fb46f1bae05952c5
  Candidate source tree:
    86df040ae5eb8e819e940dd08364bcc177a72195
  Candidate release:
    30.1.5
  Expected Core PR safety run:
    31336502539 — PENDING, NOT AUTHORIZATION
  Expected PR base:
    19baffef25af36e177db2975780e0641b59753aa
  Expected pr-gate.yml SHA-256:
    24c14f2fe4bd7b25de38e71a80bf05efcec00d2b3009c3efd4ad20b90bbda869

GitHub verifies H with the Blackcoin-Dev SSH signature and fingerprint
SHA256:jAkpBudDw+ntWHSUx3e1KY+czAFjnlaPxQtRFtptL70. Phase A and the offline
verifier require exact R to report status completed and conclusion success.
Pending, failed, cancelled, superseded-run, wrong-head, wrong-base,
wrong-workflow, and non-pull-request evidence fails closed.

Run 31336502539 is the Core qualification run, not the candidate packaging
workflow run. Candidate packaging workflow commit/run/attempt, adapter tooling
commit, bundle, OCI graph, image, provenance, and all six binary identities
remain unresolved. Phase-A/Phase-B evidence, live Compose/guard identities,
and both operator confirmations also remain unresolved. No live phase can pass
preflight.

The historical Phase-A/Phase-B filenames, v30.1.4 snapshot names, deployed
guard-compatible transaction names, immutable rollback image identities, and
immutable final-canary byte pins are intentionally unchanged.

Offline gates
-------------
PASS  Bash syntax for both phase scripts, typed_contract.sh,
      verify-evidence.sh, and candidate.env.example.
PASS  ShellCheck -x for the same shell inputs, with no findings.
PASS  Diff whitespace check for the provisional repin boundary.
PASS  Exact H/release identity and derived prefix, attempt-bound artifact,
      archive, ci1 image tag, and Phase confirmations.
PASS  Alternate 40-hex source, immutable v30.1.4 source, and nonexact release
      overrides fail the shared exact-identity predicate.
PASS  Phase A and the offline verifier both bind exact R, PR base, and
      pr-gate.yml bytes.

Offline hostile fixture suite
-----------------------------
PASS  261/261 assertions on the frozen provisional-repin bytes.
PASS  Independent hostile replay: 261/261 assertions, exit 0; exact package
      hashes were unchanged before and after the replay.

Required positive modes remain Phase-A pre-rewind, Phase-A final, and Phase-B
final with independent raw wallet-delta recomputation. New hostile evidence
must reject a different successful run, pending/null conclusion, completed
failure, wrong PR base, wrong workflow bytes, wrong head, and non-PR event.

Seal status
-----------
PASS  SHA256SUMS binds the exact paths and bytes of all eight non-seal
      payloads. Strict checksum verification, the eight-payload/nine-file
      topology, and the no-symlink/special-object boundary pass. The final
      validation bytes were resealed and replayed locally before commit; the
      independent executable review is complete.

Not executed or claimed
-----------------------
No Docker command, network mutation, GitHub mutation, workflow dispatch,
artifact build/download/load, wallet RPC, container change, ZFS operation,
live canary, or fleet action was performed. No successful Core-CI result,
candidate package, artifact, image, or release authority is claimed by this
checkpoint.
