# v30.1.5 exact-artifact publication recovery

## Scope and authorization

Blackcoin-Dev authorized one publication-only recovery and admission of the exact
signed control tag `v30.1.5-publication-recovery-1` to `production-release`.
This control branch is not runtime source and must not replace the main build
workflow. It changes no runtime, package, dependency, final tag, or release version.
The final `v30.1.5` tag remains at annotated object
`3172dd96f7963491e78e6f60f990ee11c65fa125`, targeting signed source
`1038d1eed8e699580848dcca79de8f0f60430eb6`.

## Completed evidence and publication defect

[Final run 34564576294, attempt 2](https://github.com/Blackcoin-Dev/Blackcoin/actions/runs/34564576294)
completed 35 jobs successfully. The five-platform independent byte comparison
passed in two seconds. The only failed job was publication (103216519221), after
assembly and both attestations succeeded. Its integration token could not read
the repository's immutable-release configuration endpoint (HTTP 403). The
developer-authenticated endpoint confirmed that immutable releases were enabled.
No release or draft had been created.

The committed manifest pins the complete job inventory, publisher step results,
six required artifact IDs with sizes and SHA256 digests, source run and attempt,
and existing SLSA/SPDX attestation IDs 46814847 and 46814857.

## One-shot recovery contract

The recovery accepts only a manual dispatch by Blackcoin-Dev at the exact signed
control tag, with explicit recovery acknowledgement and the existing final-release
environment acknowledgement. The control commit must directly descend from the
frozen source and modify only the enumerated publication-control files.
Local SSH verification and GitHub verification bind both source and control.

A fresh developer-authenticated immutable-configuration observation is recorded
in a receipt bound to the source, final tag object, source run/attempt, control
commit and control tag. The allowlisted release key signs that receipt using SSH
namespace `blackcoin-release-configuration`. Its maximum lifetime is one hour.
The receipt and signature are non-secret dispatch inputs; no private key or
long-lived credential is uploaded. It is checked before artifact processing and
again immediately before draft creation and publication. This narrowly replaces
only the integration-token-inaccessible configuration read.

The runner downloads the six existing artifact archives, checks their pinned
digests, and extracts them safely. Frozen-source generators reproduce the original
metadata using the original source date and run ID. Existing GitHub attestations
must bind the exact 19-subject set; the checksum file yields 20 final assets.
No build, functional test suite, or new attestation is run.

Publication fails closed unless the release is absent, the exact draft body and
all uploaded asset names/sizes/digests match, and the fresh receipt remains valid.
After publication, the release must be immutable, non-draft, non-prerelease,
latest, and still bound to the untouched final tag. A failed upload does not
authorize automatic deletion or replacement of an existing draft or release.
Remove the temporary environment admission after successful publication; preserve
the signed control tag as the immutable recovery audit record.

Blackcoin-Dev

## Executed outcome — 2026-09-11

The signed control commit is
`9909e95500b9898a764d5ea86a0b45946811d970`; its annotated control tag object is
`f36fef31aefe74085316832b750391909ee41d27`.
[Recovery run 34589845324](https://github.com/Blackcoin-Dev/Blackcoin/actions/runs/34589845324)
passed source/control authorization, artifact digest verification, deterministic
metadata reconstruction, and cryptographic verification of both complete
19-subject attestations. It uploaded all 20 assets to draft release ID
`386958149`. The run then failed because GitHub's release-by-tag endpoint returned
HTTP 404 for that unpublished draft. The numeric release endpoint returned it.
The recovery run must not be reported as wholly successful.

The same authorized publication was completed as Blackcoin-Dev using the exact
numeric release ID. No new draft, upload, build, attestation, or tag was created.
The checksum asset and one Linux probe were downloaded from that draft. Both
original attestations were cryptographically reverified against the pinned
source/tag/workflow, with the SLSA invocation fixed to original run
34564576294 attempt 2. Their full 19-subject maps matched the checksum entries and
all uploaded non-checksum asset digests. The complete 20-asset ID/name/size/digest
snapshot, canonical body, source target, final tag, live immutable setting, and
unexpired signed configuration receipt were checked before publication.

A single numeric-ID publication update completed at **2026-09-11 10:45:44 UTC**.
Read-only post-publication checks confirmed that all 20 asset identities and
digests and the body remained unchanged; the release is latest, immutable,
non-draft, and non-prerelease, and the original final tag is unchanged.

- [Final v30.1.5 release](https://github.com/Blackcoin-Dev/Blackcoin/releases/tag/v30.1.5)
- Release ID: `386958149`
- SHA256 of `SHA256SUMS.txt`:
  `21f1bf79b3a71bd10aa62adefcc7caead32f374f56212754ee1a61ea58130c08`
- Qualified source remains `1038d1eed8e699580848dcca79de8f0f60430eb6`.

Future publisher maintenance must use a numeric release ID or authenticated
release inventory for draft lookup. A release-by-tag 404 does not prove that no
draft exists. This historical one-shot control workflow is not a replacement for
the main build workflow.

Blackcoin-Dev
